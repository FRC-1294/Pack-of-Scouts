<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PackOfScouts.ScanQRCodePage"
             Title="Scanning..." >
    <Grid>
        <Grid.RowDefinitions>
               <RowDefinition Height="*"/>
               <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
               <Editor x:Name="QrData" BackgroundColor="White" TextColor="Black" TextChanged="OnQrDataTextChanged" 
                       Grid.Row="0" Grid.ColumnSpan="2" Margin="20"/>
               <Button Text="Cancel" WidthRequest="150" Clicked="OnCancelClicked" 
                       Grid.Row="1" Grid.Column="0" HorizontalOptions="End"/>
               <Button Text="Done" WidthRequest="150" Clicked="OnDoneClicked" 
                       Grid.Row="1" Grid.Column="1" HorizontalOptions="Start"/>
     </Grid>

</ContentPage>